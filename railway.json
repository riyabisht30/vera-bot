{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn bot:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/v1/healthz",
    "healthcheckTimeout": 10
  }
}
